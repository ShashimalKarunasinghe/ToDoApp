<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base App Theme (Dark Mode) -->
    <style name="Theme.ToDo" parent="Theme.Material3.Dark.NoActionBar">

        <!-- Core Colors for Dark Mode -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorError">@color/error</item>
        <item name="colorTertiary">@color/accent</item>
        <item name="colorOutline">@color/divider</item>

        <!-- Text & system surfaces -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowBackground">@color/background</item>

        <!-- Typography -->
        <item name="android:fontFamily">sans-serif</item>

        <!-- Buttons & Accent -->
        <item name="android:buttonStyle">@style/Widget.Material3.Button</item>
        <item name="android:colorAccent">@color/accent</item>

        <!-- Allow system dark mode adaptation -->
        <item name="android:forceDarkAllowed">true</item>


    </style>



</resources>
